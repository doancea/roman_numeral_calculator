#include<numeral_compressor.h>

void compressNumerals(char* numerals) {
	// int repeats = 0;
	// for(i = 0; i < strlen(numerals) - 1; i++ ) {
	// 	if(numerals[i] == 'I') {
	// 		repeats++;
	// 	}

	// 	if(repeats == 3) {
	// 		remove 
	// 	}
	// }
}